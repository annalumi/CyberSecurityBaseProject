# Cyber Security Base Project

This is a project work for HY Cyber Security Base course 

## Setup
1. Clone the repo
2. Create a virtual environment
3. Run `pip install -r requirements.txt`
4. Start the server with `python manage.py runserver`
